site_name: humble.py

use_directory_urls: false

theme:
  name: material
  features:
    - content.code.copy
    - content.code.annotate
    - search.suggest
    - search.highlight

nav:
  - Home: index.md
  - Reference: reference.md

plugins:
  - mkdocstrings:
      handlers:
        python:
          paths: ["."]
  - search
  - git-revision-date-localized